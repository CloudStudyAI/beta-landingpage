version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - |
          : > .env.production
          if [ -n "$NEXT_PUBLIC_CLOUDSTUDY_APP_URL" ]; then
            printf 'NEXT_PUBLIC_CLOUDSTUDY_APP_URL=%s\n' "$NEXT_PUBLIC_CLOUDSTUDY_APP_URL" > .env.production
          fi
          if [ -n "$NEXT_PUBLIC_WAITLIST_API_URL" ]; then
            printf 'NEXT_PUBLIC_WAITLIST_API_URL=%s\n' "$NEXT_PUBLIC_WAITLIST_API_URL" >> .env.production
          fi
          if [ -n "$NEXT_PUBLIC_TURNSTILE_SITE_KEY" ]; then
            printf 'NEXT_PUBLIC_TURNSTILE_SITE_KEY=%s\n' "$NEXT_PUBLIC_TURNSTILE_SITE_KEY" >> .env.production
          fi
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
